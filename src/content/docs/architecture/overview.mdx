---
title: Architecture Overview
description: High-level architecture of the Heyllave platform, covering the two-repository structure, request lifecycle, and key architectural patterns.
---

import FlowDiagram from '../../../components/FlowDiagram';

Heyllave is a multi-tenant SaaS platform built in Go. The system is organized into two repositories with clearly separated responsibilities and designed around a small set of composable architectural patterns.

## Repository Structure

| Repository | Purpose |
|---|---|
| `heyllave-core-service` | Main HTTP API server, all modules, and the database layer |
| `heyllave-cli` | Developer tooling: code generation, migration management, module scaffolding |
| `heyllave-admin-app` | Flutter cross-platform admin app (Android, iOS, Windows, macOS, Linux) |
| `heyllave-e2e` | Black-box E2E test suite with custom test harness |
| `heyllave-docs` | Astro/Starlight documentation site (this site) |
| `heyllave-claude-plugin` | Claude Code skills and agents for development workflows |

The API server is the runtime artifact deployed to production. The CLI is a development-time tool that reads `module.yaml` files to generate boilerplate, sync permissions, and manage schema migrations. The Flutter app is the primary user interface. Migrations live exclusively in `heyllave-cli`.

## Request Lifecycle

Every HTTP request passes through a fixed middleware chain before reaching a Huma handler, then flows downward through the service and repository layers.

<FlowDiagram
  client:visible
  direction="TB"
  height={640}
  nodes={[
    { id: 'req', label: 'HTTP Request', variant: 'accent' },
    { id: 'recovery', label: 'RecoveryMiddleware', sub: 'catches panics, returns 500' },
    { id: 'cors', label: 'CORSMiddleware', sub: 'sets CORS headers' },
    { id: 'reqid', label: 'RequestIDMiddleware', sub: 'attaches X-Request-ID' },
    { id: 'log', label: 'CanonicalLogMiddleware', sub: 'structured request/response logging', width: 240 },
    { id: 'jwt', label: 'JWTAuthMiddleware', sub: 'validates token, loads session', width: 240 },
    { id: 'ratelimit', label: 'RateLimitMiddleware', sub: 'IP · tenant · path overrides', width: 240 },
    { id: 'huma', label: 'Huma Handler', sub: 'input validation · OpenAPI binding', variant: 'coral', width: 240 },
    { id: 'service', label: 'Service Layer', sub: 'business logic · perms · audit', variant: 'accent', width: 240 },
    { id: 'repo', label: 'Repository Layer', sub: 'SQL via sqlx + squirrel', variant: 'accent', width: 240 },
    { id: 'pg', label: 'PostgreSQL', sub: 'tenant schema via search_path', variant: 'db', width: 240 },
  ]}
  edges={[
    { source: 'req', target: 'recovery' },
    { source: 'recovery', target: 'cors' },
    { source: 'cors', target: 'reqid' },
    { source: 'reqid', target: 'log' },
    { source: 'log', target: 'jwt' },
    { source: 'jwt', target: 'ratelimit' },
    { source: 'ratelimit', target: 'huma' },
    { source: 'huma', target: 'service' },
    { source: 'service', target: 'repo' },
    { source: 'repo', target: 'pg' },
  ]}
/>

All request-scoped data (current user, tenant, permissions, request ID) is available through the `Core` struct passed into every service and handler. Handlers never reach into the database directly; all data access goes through the repository layer.

## Key Architectural Patterns

### Dependency Injection via the Core Struct

The `Core` struct is the single dependency carrier for the entire application. It holds 11 sub-interfaces covering session management, permissions, auditing, events, modules, hooks, storage, database, configuration, error construction, and logging.

Services and handlers receive `*Core` at initialization and use it to access every platform capability. This makes every dependency explicit and every component independently testable via mock implementations.

See [Core Struct](/architecture/core-struct) for the complete interface definitions.

### Schema-per-Tenant Isolation

Each tenant is assigned a dedicated PostgreSQL schema named `tenant_<slug>`. At the start of each authenticated request, the database connection sets:

```sql
SET search_path = tenant_slug, public;
```

All module tables live in the tenant schema. Shared platform tables (users, tenants, memberships, roles) remain in the `public` schema and are accessible to all schemas via the search path fallback.

See [Schema-per-Tenant](/architecture/schema-per-tenant) for isolation details and trade-offs.

### Transactional Outbox for Domain Events

Domain events are not emitted directly to an in-memory bus. Instead, `EventBus.EmitTx` writes the event record into an outbox table within the same database transaction as the originating business operation. A background processor polls the outbox and delivers events to subscribers.

This guarantees that an event is never lost if the process crashes after the DB commit but before an in-memory delivery would have occurred.

See [Event Outbox](/architecture/event-outbox) for the full delivery and retry model.

### RFC 6902 Audit Trail

Every state-changing operation records an audit entry. Updates produce a JSON Patch diff (RFC 6902) between the before and after snapshots, making it possible to replay or inspect every change to any resource over time.

See [Audit Trail](/architecture/audit-trail) for the recording API and query model.

## Module Types

Heyllave organizes all functionality into modules. Every module follows the same interface and file layout convention.

| Type | Purpose | Examples |
|---|---|---|
| `foundation` | Core platform capabilities required by all tenants | users, tenants, audit, auth |
| `domain` | Business-specific functionality bundled with the platform | repairs, inventory, billing |
| `custom` | Tenant-specific extensions developed per-customer | custom workflows, integrations |

Foundation modules are always enabled. Domain and custom modules can be enabled or disabled per tenant through the module registry.

See [Module System](/architecture/module-system) for lifecycle, registration, and the `module.yaml` configuration format.

See the [Architecture Diagrams](/architecture/diagrams) page for comprehensive visual diagrams of the entire platform.

## Technology Stack

| Layer | Technology |
|---|---|
| Language | Go 1.25 |
| HTTP framework | [chi](https://github.com/go-chi/chi) + [Huma](https://huma.rocks/) (OpenAPI-first) |
| Database | PostgreSQL 15+ (schema-per-tenant) |
| SQL builder | pgx + sqlx + squirrel |
| Auth | JWT + OAuth2/OIDC + WebAuthn (FIDO2) + Magic Links |
| Password hashing | bcrypt (cost 12) |
| Events | Transactional outbox with background worker |
| Notifications | In-app, Email (SMTP), Slack, Webhook |
| Hooks | Webhooks + Expression language (expr-lang) |
| Storage | Local filesystem + AWS S3/MinIO |
| Config | Viper (YAML + env vars + flags) |
| Logging | slog + charmbracelet/log |
| CLI | Cobra + Viper + Atlas + Docker SDK |
| Admin App | Flutter 3.27+ / Dart 3.6+ / Riverpod |
| Docs | Astro 5 + Starlight |
| Testing | testify + testcontainers-go + custom E2E harness |
